# VELORA — Capacitor iOS → App Store Connect / TestFlight
#
# المشروع: Next.js + Capacitor (غلاف أصلي يفتح https://velorabeautyiq.me)
# Xcode: ios/App/App.xcodeproj — scheme: App — bundle: beauty.velora.app
# Capacitor: SPM (لا CocoaPods) — webDir: mobile-www
#
# توقيع iOS: تلقائي عبر تكامل App Store Connect (اسم التكامل: codemagic)
# + app-store-connect fetch-signing-files --create أثناء البناء.
# لا تستخدم environment.ios_signing هنا — يتطلب profiles مسبقة في
# Codemagic → Code signing identities ويسبب:
# "No matching profiles found for bundle identifier ..."
#
# قبل أول build: راجع scripts/CODEMAGIC-SETUP.md

workflows:
  velora-ios-testflight:
    name: VELORA iOS → TestFlight
    max_build_duration: 120
    instance_type: mac_mini_m2

    integrations:
      app_store_connect: Codemagic VELORA

    environment:
      groups:
        - velora
      vars:
        BUNDLE_ID: beauty.velora.app
        APPLE_TEAM_ID: UC7FV2YW74
        # distribution_type: app_store (يُطبَّق في fetch-signing-files --type IOS_APP_STORE)
        XCODE_PROJECT: ios/App/App.xcodeproj
        XCODE_SCHEME: App
        VELORA_MOBILE_URL: https://velorabeautyiq.me
        NODE_OPTIONS: --max-old-space-size=4096
        # عيّن في Codemagic UI → مجموعة velora (Secret — لا تضع القيم في Git):
        # DATABASE_URL — مطلوب لـ npm run build (prisma migrate deploy)
        # CERTIFICATE_PRIVATE_KEY — مطلوب (مفتاح RSA كامل BEGIN/END)
        # إن فشل التكامل: أضيفي أيضاً APP_STORE_CONNECT_ISSUER_ID، APP_STORE_CONNECT_KEY_IDENTIFIER، APP_STORE_CONNECT_PRIVATE_KEY
        # APP_STORE_APP_ID — الرقم من App Store Connect → App Information

      # Capacitor CLI 8.4+ يتطلب Node >= 22 (bin/capacitor يفشل على Node 20)
      node: 22
      xcode: latest

    triggering:
      events:
        - tag
      tag_patterns:
        - pattern: "release/*"
          include: true

    scripts:
      - name: Install npm dependencies
        script: |
          set -e
          npm ci

      - name: Build web application
        script: |
          set -e
          if [ -z "$DATABASE_URL" ]; then
            echo "ERROR: DATABASE_URL is not set in Codemagic environment variables."
            exit 1
          fi
          npm run build

      - name: Sync Capacitor with iOS
        script: |
          set -e
          export VELORA_MOBILE_URL="${VELORA_MOBILE_URL}"
          npx cap sync ios

      - name: Set up keychain for code signing
        script: |
          set -e
          keychain initialize

      - name: Verify Apple signing prerequisites
        script: |
          set -e
          echo "=== Apple signing prerequisites (values hidden) ==="
          if [ -n "${CERTIFICATE_PRIVATE_KEY:-}" ]; then echo "CERTIFICATE_PRIVATE_KEY: set"; else echo "CERTIFICATE_PRIVATE_KEY: MISSING"; fi
          if [ -n "${APP_STORE_CONNECT_ISSUER_ID:-}" ]; then echo "APP_STORE_CONNECT_ISSUER_ID: set"; else echo "APP_STORE_CONNECT_ISSUER_ID: missing (needs integration Codemagic VELORA or velora group)"; fi
          if [ -n "${APP_STORE_CONNECT_KEY_IDENTIFIER:-}" ]; then echo "APP_STORE_CONNECT_KEY_IDENTIFIER: set"; else echo "APP_STORE_CONNECT_KEY_IDENTIFIER: missing"; fi
          if [ -n "${APP_STORE_CONNECT_PRIVATE_KEY:-}" ]; then echo "APP_STORE_CONNECT_PRIVATE_KEY: set"; else echo "APP_STORE_CONNECT_PRIVATE_KEY: missing"; fi
          if [ -z "${CERTIFICATE_PRIVATE_KEY:-}" ]; then
            echo "ERROR: CERTIFICATE_PRIVATE_KEY is not set in Codemagic (group: velora, Secret)."
            exit 1
          fi
          if [ -z "${APP_STORE_CONNECT_ISSUER_ID:-}" ] || [ -z "${APP_STORE_CONNECT_KEY_IDENTIFIER:-}" ] || [ -z "${APP_STORE_CONNECT_PRIVATE_KEY:-}" ]; then
            echo "ERROR: App Store Connect API credentials are not available to the build."
            echo "Fix: Codemagic Team settings → Integrations → Developer Portal → key name must match yaml: Codemagic VELORA"
            echo "Or add APP_STORE_CONNECT_ISSUER_ID, APP_STORE_CONNECT_KEY_IDENTIFIER, APP_STORE_CONNECT_PRIVATE_KEY to group velora."
            exit 1
          fi

      - name: Fetch signing files from Apple Developer Portal
        script: |
          set -e
          CERT_KEY_PATH="$CM_BUILD_DIR/ios_distribution_key.pem"
          # Codemagic secrets may store PEM with literal \n — %b decodes them
          printf '%b\n' "$CERTIFICATE_PRIVATE_KEY" > "$CERT_KEY_PATH"
          chmod 600 "$CERT_KEY_PATH"
          if ! app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --type IOS_APP_STORE \
            --certificate-key=@file:"$CERT_KEY_PATH" \
            --create; then
            echo "fetch-signing-files --create failed — retrying fetch without --create (use existing profile/cert)..."
            app-store-connect fetch-signing-files "$BUNDLE_ID" \
              --type IOS_APP_STORE \
              --certificate-key=@file:"$CERT_KEY_PATH"
          fi

      - name: Add certificates to keychain
        script: |
          set -e
          keychain add-certificates

      - name: Set up code signing settings on Xcode project
        script: |
          set -e
          xcode-project use-profiles

      - name: Increment build number
        script: |
          set -e
          cd ios/App
          if [ -n "$APP_STORE_APP_ID" ]; then
            LATEST_BUILD_NUMBER=$(app-store-connect get-latest-app-store-build-number "$APP_STORE_APP_ID")
            agvtool new-version -all $(($LATEST_BUILD_NUMBER + 1))
          else
            echo "APP_STORE_APP_ID not set — using Codemagic BUILD_NUMBER ($BUILD_NUMBER)"
            agvtool new-version -all "$BUILD_NUMBER"
          fi

      - name: Build ipa for distribution
        script: |
          set -e
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
